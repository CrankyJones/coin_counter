user_input = .51
output = 2 quarters + 1 penny

whatever user_input = subtract largest values and whatever is left add smaller coin values
.30 = 

[quarters, dimes, nickels, pennies]

value_hash => {['quarter': 25, 'dime':10, 'nickel':5, 'penny':1]}

value_hash.each { }
    loop 
      if purse >= value_hash
        purse - value_hash
        #{key}_count += 1
value_hash.each
       if (#{key}_count > 0)
      result.concat("#{{key}_count} #{key}(s) ")
    end


#{@change_purse.keys[@counter]}

@change_purse.keys = ['quarter', 'dime', 'nickel' , 'penny']
@counter starts at 0 in the initialize
  and gets a +1 

using @counter to point to an index of the array that is returned by @change_purse.keys

new_array[0]